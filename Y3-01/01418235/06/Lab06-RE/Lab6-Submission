รหัสนิสิต: 6410451199
ชื่อ: พศวัต ถิ่นกาญจน์วัฒนา

เขียนคำตอบของแต่ละข้อต่อไปนี้ ลงต่อท้ายเลขข้อที่กำหนดให้ 
คำตอบสำหรับข้อ ...
4.1 grep '\(\<[A-Za-z]\+\>\).*\<\1\>' dos-and-unix
4.2 grep '\(\<[A-Za-z]\+\>\).*\<\1\>.*\<\1\>' dos-and-unix

5.1 grep -x '^\*[^\*].*' asterisks.txt
5.2 grep '\*[^\*]\*' asterisks.txt
5.3 grep '[\*].*' asterisks.txt | grep -v '[\*]\{4,\}'
5.4 grep '^\([*]\+\).*[^*]\1$' asterisks.txt
